apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/part-of: cisc814-lab
  app.kubernetes.io/name: sample-web

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
